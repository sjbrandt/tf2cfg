// Binds all movement keys to also call for medic
// For when a medigun is named "I hope your E key breaks" or similar

alias call "voicemenu 0 0"

bind w "+forward;call"
bind a "+moveleft;call"
bind s "+back;call"
bind d "+moveright;call"